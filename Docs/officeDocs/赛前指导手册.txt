
这次大赛每个组有一个GPU设备使用，是P100目前最强的GPU
每个参赛队伍登录后都可以看到自己的设备
每个队伍的GPU是独享的
这次大赛主要比试 object detection的问题
主题是找出这个人是否“”有意遮挡脸部“”
标签两种 face, mask
训练数据大小是千张级别，不是很多，所以12个小时是可以训练到一个比较好结果的。大家要合理安排时间
我们为大家准备好了PowerAI，这是一套开源深度学习的框架的集合包，包括caffe, tensorflow, torch, theano
大家可以直接使用这个框架，也可以编译你们自己的发行版本。每台机器都是可以自由连接外网的，而且不用翻墙
操作系统是ubuntu 16.04
脸部遮挡的标准，大家到时候可以看图片数据，大的原则就是挡住了脸部主要特征
包头巾是不算遮挡，但是带墨镜算遮挡
侧脸不算
tf的版本是0.12，当然，如果你想使用1.0也可以自己从github上拖下来
不过大家注意，比赛的底层硬件是基于Power的而不是基于x86的，所以，编译的时候可能会有依赖问题，需要解决
训练的都是人脸，当然会有一些背景，不过考虑到时间问题，不会穿插特别的物体
可以使用已经有的权重文件
环境基本不需要配置，如果你是用我们提供的框架

1. 可以使用模式识别的方法来辅助你的训练，但是如果最后得分相同的情况下，使用纯deeplearning方式的会判定为胜出
2. 大赛评分以map, iou来评判准确率
3.大赛以validate所有数据集的时间判定performance
4. 大赛鼓励大家设计一些新的模型，比如如果你可以只是用我们提供的标签一半，或者更少的标签，可以加分
第四项是鼓励大家使用一些比较新的技术，比如ladder network的方式
训练数据不减少，只是减少label
比赛提交的文件，会在当天的开场的时候介绍
分值是分享计分，到时候累加
IOU 65%以下都是0分，这一项
mAP60%以下都是0分，准确率这一项
编程语言不限制
可以使用自己的改造过的framework，只是编译问题自己解决
服务器， GPU P100,128G内存，CPU P8 4Core
显卡 P100 目前最尖端的GPU了，所以这个你们没有什么好担心的
必须在大赛环境里面跑validation
机器里面应该没有anaconda，如果需要自己可以装
比赛12个小时，收敛到一个合理的值，8小时肯定足够了，所以是有足够时间支撑你们做需要的事情
任何修改的代码，数据，模型，你们提交的时候要说明。原则上是可以的，但是如果你们不说明，被查出来，结果会被判无效
可以用自己修改的发行版，github上别人提供的发行版，都可以,但是依赖问题和编译问题要自己解决
注意这个是Power平台，不是x86,所以如果你是在x86上编译的目标代码肯定不能直接到power上运行的
现在不提供环境登录，要在比赛前发放登录密码，如果你想试可以去www.nimbix.net，不过一小时5美金
登录都是ssh，自备client
尺寸不一致，jpg和png，输出xml
测试级是独立的，比赛结束前30分钟发放
对图像的缩放、剪切，扣分不扣分
对于object detection，业界有两种主流的方式，一种很明显有性能优势，另一种在某些情况下准确率更高，大家也可以随意挑选
ssh登录，所以你也可以sftp上传下载
Power支持Container，但是如果你的container是x86的是不能在power上运行的
bbox尺寸不一样
scp什么的都没有问题了，就是标准linux环境
会有多个object，有sudo的权限
